# Dependências do projeto — ambiente testado: conda "tcc", Python 3.10.
# Versões fixadas para reprodutibilidade. Instalação e notas de GPU: ver README.md.
# IMPORTANTE: manter numpy < 2 — o TensorFlow 2.10 quebra com numpy >= 2.

kagglehub==1.0.1      # download do dataset Spotify Million Playlist
pandas==2.3.3         # manipulação dos DataFrames
numpy==1.26.4         # operações numéricas (NÃO subir para 2.x — ver README)
tqdm==4.67.3          # barras de progresso
scikit-learn==1.7.2   # StandardScaler, MultiLabelBinarizer, cosseno, train_test_split
scipy==1.15.2         # matrizes esparsas (npz, álgebra esparsa)
joblib==1.5.3         # serialização do cache e do modelo content
implicit==0.7.3       # ALS (Matrix Factorization) + item-kNN por co-ocorrência
tensorflow==2.10.0    # rede neural NeuMF (última versão com GPU nativa no Windows)
keras==2.10.0         # API usada para construir a rede
